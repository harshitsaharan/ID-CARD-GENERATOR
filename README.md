# ID-CARD-GENERATOR
The ID Card Generator is a React-based web app that lets users customize ID cards using predefined templates. It allows editing of name, college/team details, text position, color, and fonts with a live preview. The final design can be exported as a print-ready PDF or high-quality image.
